# 引き継ぎメモ

**別の環境で作業を再開するとき、最初に読む文書。**

他のドキュメントと役割を分けてある:

| 文書 | 内容 |
|---|---|
| [`plan.html`](plan.html) | 全体計画とフェーズ構成 (**正は Claude の Artifact** — 下記 §2) |
| [`research.md`](research.md) | 楽器の物理と手法選定の根拠 |
| [`problems.md`](problems.md) | 既知の問題・意図的に諦めた近似 (D-001〜) |
| **このファイル** | **現在地、判断の理由、環境、ハマりどころ** |

コードとコミットログから読み取れることは書かない。**読み取れないこと**だけを書く。

---

## 1. 現在地

**Phase 6 (ROOM — X-Y ステレオ) まで完了。**

| Phase | 内容 | 状態 |
|---|---|---|
| 0 | ワークスペース骨組み / ドキュメント / render・analyze | ✅ |
| 1 | 1 区間の弦 + 硬い撥 (modal / hammer / segment) | ✅ |
| 2 | CLAP 化 (instrument / plugin / ABI テスト) | ✅ (実機確認 2 回済み) |
| 3 | 楽器全体 (layout / scaling / ベンチ) | ✅ |
| 4 | ブリッジ結合・弦 2 本・うなり | ✅ |
| 5 | 響板と箱 (engine / soundboard / cabinet) | ✅ |
| 6 | ROOM — X-Y ステレオ (room / パラメータ 5 つ) | ✅ |
| 7 | **演奏表現 (撥の面・ミュート・音律・配置切り替え)** | ✅ (2026-08-31、コア 4 項目) |
| 8 | 最適化 (active スキップ、D-027) | ✅ (2026-09-01) |
| 9 前半 | **GUI (egui エディタ — 鍵盤 + マイクステージ)** | ✅ (2026-08-31、DAW 実機確認待ち) |
| 9 後半 | プリセット / state 拡張 | 未着手 |
| 10 前半 | **音域バランス (LUFS 校正)** | ✅ (2026-08-31) |
| 10 後半 | **撥の面のラウドネス補償 (D-026)** | ✅ (2026-09-01) |
| 10 後半 | **半音階に低音弦ブロック D#2–D#3 (ブロンズ巻) を追加** | ✅ (2026-09-01) |

テスト 250 件、`cargo clippy` / `cargo fmt` ともにクリーン。

### 低音弦ブロックで決めたこと (2026-09-01)

- **半音階配置は D#2–E6 の 50 音・61 位置になった** (`LayoutKind::Chromatic`
  に改名。パラメータ値 0/1 と `layout_code` は不変なので DAW 互換)。
  バスブリッジは 25 コース: **低音弦ブロック D#2 (39)–D#3 (51) の 13 コース
  + 既存 E3–D#4 の 12 コース**
- **既存ブロックの弦設計は 1 bit も変えない**が不変条件。ブロックは
  `scaling::BassExtension` の**別の直線**で補間する (1 本の直線で 25 コースを
  結ぶと既存弦長が動き、D-022 の校正がやり直しになる)。線径の音域補間
  (`DesignContext::key_min`) もブロックを除いた範囲のまま
- ブロックのジオメトリ: 弦長 900→580 mm (E3 の 550 に接続)、芯線
  0.60→0.56 mm、巻線倍率 w 4.2→2.5 (**全コース巻線 = ブロンズ巻の写し**。
  モデル上、巻の材質は線密度にしか効かないので専用の材質定数は無い)。
  D#2: T 184 N / 650 MPa / B 8.4e-5 / 165 モード / T60 14 s — published-ranges
  テストの範囲内
- **LUFS 校正**: 補正表 (D-022) にブロック 13 鍵のアンカーを追加。補正前
  −5.1〜+3.3 LU → 補正後**全 50 鍵が ±0.79 LU** (A4 −23.9 は不動)。
  面の補償 (D-026) は 98 Hz 未満で端に張り付くが、低音は面の音量差が
  そもそも小さい (フェルト D#2 で −0.9 LU) ので追加の表は不要
- GUI の鍵盤は D#2–E6 の 50 鍵 (白鍵 29 本) に拡張。15/14 では下端 4 鍵が
  暗く沈む
- CPU (参考、開発機 = 基準マシンより遅い): 半音階全 50 ff のベンチが
  661〜785 µs / 締切 1333 µs (計測ぶれあり)。長い低音弦は反射が遅く
  (2L/c ≈ 13 ms)、撥のチャタリング = 16 倍オーバーサンプル区間が伸びる
  ぶん増えた。ユーザー確認済み: 最悪ケースの増は許容
- **無音時が実害になった** (エディタ表示中は眠らない D-025 と重なり、
  エディタを開いただけで GUI が重い) → P8 の active スキップで解消
  (下記、D-027)

### Phase 8 で決めたこと (active スキップ、2026-09-01)

- **区間単位のスリープ**: 出力ピーク < 1e-7 N (≈ −140 dBFS) が約 170 ms
  続いたら眠る。眠っている間はバンクを回さず厳密に 0、`Instrument` は
  ブロックごと飛ばす。起きる契機は打弦とブリッジ駆動 (5 度の共鳴を
  殺さないため) だけ → [D-027](problems.md)
- **実測** (開発機): 無音の 15/14 楽器 341 µs → 0.1 µs、無音の半音階
  エンジン 502 µs → **32 µs** (残りは響板 ×2 + 箱 + ROOM)。全打鍵の
  最悪ケースは不変
- 鳴っている弦は眠らない — ダンパーの無い楽器の物理には触れていない。
  SIMD 化は PhyPiano から移植済みなので、P8 の残件は無し
- GUI の鍵盤上のラベルをハードコードから配置表由来に修正
  (低音弦ブロック追加時に「E3 - E6 - 37 NOTES」のまま取り残された実害)

### Phase 10 後半で決めたこと (面のラウドネス、2026-09-01)

- **実測が先**: レザー・フェルトの木との差は音域に強く依存する — バスは
  ほぼ差なし、トレブルは 10–21 LU 凹む (柔らかい面は接触が長く、短い弦では
  高次を励振できない。物理としては実機どおり)。速度依存も大きい
  (トレブルで最大 8 dB) → 静的ゲインの校正は **vel 1.0 / 0.5 の 2 点平均**
- **補償は打った区間の f0 で引く** (`scaling::face_gain`、面ごとの
  アンカー表 = 15/14 全 27 鍵)。コースゲインと違い**位置ごと** — 左区間は
  右と別の f0 で、左右で挙動も違う
- **励振への掛け方は等価変換** (`Segment::set_strike_gain`): 注入力を g 倍、
  撥が見る弦変位を 1/g。接触の動力学は g = 1 と厳密に同一のまま出力だけ
  g 倍 (回帰: `strike_gain_scales_the_output_exactly`)。単純な注入 g 倍は
  弦が早く「逃げ」て音色が変わるので不可
- **補償後の残差**: 両速度でおおむね **±3 LU** (フェルト全鍵 ±2.9)。
  レザー F#5 だけ ±7.3 LU (2 速度の差が 14.5 dB ある特異点) → [D-026](problems.md)
- render の `--instrument` / `--sweep` に `--face` を配線した (測定入口)

### Phase 9 前半で決めたこと (GUI)

- **クレート分割**: `phydulcimer-gui` は egui + core のみでウィンドウを
  開かない (テスト可能・依存封じ込め)。埋め込み (egui-baseview) は
  plugin 側の `editor.rs` だけが持つ。**PhyPiano から複製、パス依存なし**
- デザインは Artifact「PhyDulcimer GUI」(vim-hybrid ダーク + 木のアクセント、
  960×640 固定)。鍵盤のバンク色は暖色類似色ランプ (赤/橙/黄)、打鍵グローは
  ウォームホワイト 300 ms。鍵盤の色分け・鍵域は **active 側の Layout** で描く
  (パラメータだけ変えた状態では鳴る配置はまだ変わっていない)
- スレッド間はロックなし 3 経路 + Sleep 抑止 → [D-025](problems.md)。
  DPI は PhyPiano の既知の歪みを継承 → [D-024](problems.md)
- GUI の実機確認は DAW でユーザーが行う (README のチェックリスト)。
  自動テストは gui クレートの描画・純関数と ABI (拡張宣言・ID 一致) まで

### Phase 7 で決めたこと (演奏表現、コア 4 項目)

- **スコープ**: 撥の面・パームミュート・ブリッジ位置 (音律)・配置切り替えの
  4 項目 + 完了条件 (打弦点掃引)。**ダンパーペダルとハーモニクスは見送り**
  (設計が薄い。ハーモニクスは `ModalBank::set_mode_damping` で実現可能な
  素地だけある)。マイクロチューニングは計画どおり保留
- **指標を先に作った**: `analyze --centroid` (部分音重心 — FFT 全帯域はノイズ
  床に汚れるので、`find_partial` の走査で実在部分音だけを拾う)。完了条件は
  `tests/strike_sweep.rs` が固定 — 重心 1500 Hz (x/L 0.03) → 897 Hz (0.30)、
  x/L = 1/整数 の点でノッチが部分音を丸ごと消す**コム通過リップル** (+63 Hz)
  は物理として許容
- **撥の面**: `HammerParams::for_face` は P1 から存在、セッターの配線のみ。
  次の打撃から効く (係数再構築は strike_pair の遅延実行)。param 8 + CC70
- **ミュート**: `ModalBank` のモード別減衰 (P7 予約済みだった) の配線。
  再構築なしで鳴っている弦に即効く。カーブは校正値 → [D-023](problems.md)。
  param 9 + CC1
- **音律**: `TREBLE_LONG_SHARE = 0.6` を `Temperament` (Pure 0.6 / Equal
  r/(1+r)) に昇格。左区間の周波数は分割比から導かれるまま (物理を曲げない)
- **配置**: `scaling::design_position` の `55 + course_offset` ハードコードを
  廃止し、音高を配置表から取る形へ (`design_position_with` + `DesignContext`)。
  半音階 E3–E6 はバス 52..=63 + 右 64..=81 + 左 +7 = **37 音 48 位置**。
  ジオメトリは published-ranges テストが外部基準。音域バランスは
  15/14 表 + 補正表 → [D-022](problems.md)
- **activate 時適用**: Layout/Temperament は弦バンク再構築を伴うので
  activate で読む。変更検出時は `host.request_restart()` を一度だけ要求
  (非対応ホストは次の activate で反映)。`assert_no_alloc` は process のみを
  包むので activate 内の確保は検査に掛からない (設計どおり)

### Phase 10 前半で決めたこと (音域バランス)

- **指標を先に作った**: `analyze --lufs` (BS.1770-4、K 特性 + ゲーティング。
  48 kHz で規格の係数表と 1e-6 一致をテストで固定)。単音の音域比較は
  **モーメンタリ最大** — ゲート付き積分値はレンダリング長と T60 に依存する
  (バス 12 s / トレブル 3 s のこの楽器では低音を過大に読む)
- **補償はコース単位の出力ゲイン** `scaling::course_gain(f0)`。
  (log2 f, dB) 折れ線 14 アンカー、**440 Hz = 0 dB** で A4 の校正は不動。
  励振・結合・減衰の物理には触れない。ハンマー質量のピッチ依存 (物理的には
  正しい) は音色が変わるので Phase 7 へ保留 → 経緯と退けた案は
  [D-021](problems.md)
- **ターゲット**: `LUFS(A4) + 1.0 LU/oct · log2(f0/440)`、許容 ±2.5 LU。
  補償前 18.3 LU の広がり → 補償後は全 27 鍵が **±2.0 LU** (広がり 4.8 LU)
- **アンカー表は校正値**。響板・箱・撥を触ったら
  `render --sweep` + `analyze --lufs` の掃引からやり直すこと。
  回帰は `tests/register_balance.rs` (代表 7 鍵) が固定

### Phase 6 で決めたこと

- **X-Y の 3 性質を構造で保証**: どの層にも L/R 間の遅延差が無い (反射タップは
  1 本の遅延線 + ゲイン分配、FDN の相関は出力ミクスの内積 ρ=0.7 で設計)
- 幾何は `room.rs` に固定: バスブリッジ −0.35 m / トレブル +0.25 m、
  マイク正面 d、鏡像法 12 タップ/音源、FDN 8 ライン
- パラメータ: Room (on/off) / Mic Distance / X-Y Angle / Room Size / Wall
  Absorption。**ROOM の同期もイベント処理の後** (D-015 の教訓を踏襲)
- **音質の測定・調整は必ず Room off** (`render --no-room`)。部屋は粗を隠す

### Phase 5 で決めたこと

- **経路**: Instrument → 2 バス (バス/トレブルブリッジ) → Soundboard × 2 ゾーン
  + Cabinet (双峰 110/195 Hz) → tanh ソフトクリップ → L=R。
  `DulcimerEngine` が束ね、プラグインと render --instrument が共用する
- **響板は ModalBank の再利用** (200 モード/ゾーン、対数間隔+ジッタ、
  減衰散らし上限クランプ)
- 校正の数値と経緯は [D-020](problems.md)。**IR の校正入口は
  `render --soundboard` → `analyze --bands`**
- 校正: ff 単音 −10 dBFS / ff 4 音和音 0.72 / 全 44 ff はクリップが受ける。
  クレストは響板で**下がらなかった** (39→125、D-013 の予想は外れ — D-020)

### Phase 4 で決めたこと

- **ブリッジ結合は撥の接触力の順方向注入** ([D-018](problems.md))。遅延帰還は
  2 案とも実測で発散 — この楽器の弦は減衰が軽すぎて、明示的帰還では結合できない。
  真の双方向 (陰的 1×1) は将来の仕事として D-018 に仕様を残した
- **コースあたり弦 2 本** (+0.8〜1.8 cent デチューン、打撃 0–0.3 ms ばらつき)。
  うなりは `analyze --modulation` で 32–36 dB のリップルとして実測
- **デノーマル対策の DC 注入 1e-15** ([D-019](problems.md))。回収の無い楽器は
  構造的にデノーマルに沈む (ボイス回収のあるシンセとは逆の結論になる)
- 区間の総数 88。最悪ケース CPU **439 µs = 33%** (P8 の active スキップで削る)

DAW の実機確認は 2 回実施済み (D-016 を発見→修正→再確認 OK)。
**Phase 3 で鍵の配置が変わった** (下記) ので、DAW で触るとき驚かないこと。

### Phase 3 で決めたこと

- **15/14 標準配置** (`layout.rs`): バス G2–F#4 / トレブル右 G3–G5 /
  トレブル左 D4–D6。44 位置・27 音高。**半音の欠落は仕様** ([D-017](problems.md))
- **設計則** (`scaling.rs`): 台形の幾何 (トレブル全長 826→360 mm) +
  応力目標 650 MPa からの巻線導出 + 音域別の線径 (0.55→0.35 mm)。
  `render --table` で 44 位置の表が出る
- **トレブル左は同じ弦の短い区間**。周波数は物理から導かれ (純正5度 = 右×1.5)、
  平均律より +2 cent。左にしか無い音 (C#5, A5, B5, C#6, D6) だけがそれで鳴る
- 重複音高の優先: バス → トレブル右 → トレブル左 (最も長い区間)

Phase 1 で決めたこと (数値の根拠は [problems.md](problems.md)):

- **オーバーサンプルは 16 倍、デシメーションフィルタは不要** ([D-010](problems.md))
- **撥の剛性は接触時間からの校正値**: 木 1e8 / レザー 3e8 / フェルト 4.5e9 ([D-011](problems.md))
- **木の撥は再接触を許す** (チャタリング)。「最初の離脱で終わり」は
  フェルト専用の仮定だった ([D-012](problems.md))
- **モードは絞らない**。打ち切りはチャタリング経由で低次まで動かす ([D-010](problems.md))
- SIMD は Phase 8 を待たず移植済み ([D-008](problems.md))。Phase 8 の中身は実測とチューニングになる

---

## 2. 環境

- ツールチェイン: Rust (`rust-version = "1.85"`, edition 2021) で確認
- リモート: `https://github.com/TY17-write/Rust-CLAP-PhyDulcimer.git`
  (ブランチ `main`、upstream 設定済み)
- **計画の正は Claude の Artifact**:
  https://claude.ai/code/artifact/a650768b-6e46-4ba6-a022-0a3ab186990d
  (所有者のみ閲覧可)。計画を直すときは Artifact 側を更新してから
  `docs/plan.html` へコピーし直す。新しい Artifact を作るとリンクが分裂する

### 隣のプロジェクト

- `../egui-clap-host` — 音を出して確かめるときのホスト
- `../trisphere` — clack の書き方の手本。revision `c5975f9` を揃える

---

## 3. 検証のやり方

```bash
cargo test --workspace
cargo clippy --workspace --all-targets
cargo fmt --all -- --check
```

オフライン検証 (Phase 0 時点):

```bash
# 単一の減衰正弦波
cargo run --release -p phydulcimer-render -- --out out/smoke.wav --freq 440 --t60 2.0 --dur 3.0
cargo run --release -p phydulcimer-analyze -- --in out/smoke.wav --f0 --t60 --freq 440

# 部分音列 (インハーモニシティと減衰の傾きつき)
cargo run --release -p phydulcimer-render -- --out out/tone.wav \
    --freq 220 --partials 8 --t60 3.0 --t60-taper 1.0 --inharmonicity 4e-4 --dur 4.0
cargo run --release -p phydulcimer-analyze -- --in out/tone.wav \
    --partials 220 --count 8 --partial-t60 --window 2.0
```

音域バランスの掃引 (Phase 10):

```bash
# 全 27 鍵を 1 鍵 1 ファイルで out/sweep/key-<midi>.wav へ (ff, ROOM off)
cargo run --release -p phydulcimer-render -- --instrument --sweep --out out/sweep/key.wav --dur 3.0 --vel 1.0 --no-room
# 各ファイルの LUFS (モーメンタリ最大で比べる)
cargo run --release -p phydulcimer-analyze -- --in out/sweep/key-69.wav --lufs
```

半音階は `--layout chromatic` を付ける (render の --sweep / --table / --instrument 共通)。
打弦点の指標は `analyze --centroid --f0ref <HZ>` (部分音重心、Phase 7)。

### 測るときに知っておくこと

- **`--peak` の既定は 0 (正規化しない)** ([D-004](problems.md))。
  聴きたいときだけ `--peak 0.9` を付ける
- **Goertzel は窓の中の平均振幅を返す。** 減衰する信号を全区間で測ると小さく出る
  (3 秒窓で −37.7 dB、先頭 0.2 秒窓で −8.95 dB、設計振幅 0.5)。**窓を切ること**
- **部分音は走査で見つける。** インハーモニシティ B を知らなくてよい。B は結果として
  報告される
- **`--partial-t60` は T60 が 0.4 秒を切ると沈黙する** ([D-001](problems.md))。
  「減衰が無い」ではない
- **`--lufs` は WAV の全チャンネルをそのまま測る** (`--channel` と独立)。
  モノ平均を先に取ると約 3 dB 低く出る。単音の比較は**モーメンタリ最大**で
  行い、ゲート付き積分値をレンダリング長の違う音源間で比べない ([D-021](problems.md))

---

## 4. 実測値 (回帰の基準)

**大きく変わったら何かを壊している。**

### Phase 0 — 測定器そのもの

| 項目 | 設計値 | 実測 |
|---|---|---|
| f0 推定 (自己相関) | 440 Hz | **440.00 Hz** |
| 信号全体の T60 | 2.0 s | **2.000 s** (R² = 0.9999) |
| 部分音 T60 (n = 1 / 2 / 4) | 3.0 / 1.5 / 0.75 s | **3.000 / 1.500 / 0.750 s** |
| 部分音位置 (n = 8, B = 4e-4) | +21.9 cent | **+22.2 cent** |
| インハーモニシティ B の推定 | 4.0e-4 | **3.878e-4** (誤差 3%) |
| WAV 往復 (32-bit float) | — | **標本が完全一致** |
| `--partial-t60` の下限 | — | **0.5 s は可 / 0.375 s は不可** ([D-001](problems.md)) |

### Phase 1 — 弦と撥 (treble-long: L=495 mm, D4=293.66 Hz, d=0.5 mm)

| 項目 | 設計値 | 実測 |
|---|---|---|
| 張力 / 応力 | — | **130.3 N / 663 MPa** (music wire の実用域) |
| インハーモニシティ B | 1.897e-4 | **1.925e-4** (誤差 1.5%、アナライザ経由) |
| 部分音の伸び (n=10) | +16 cent | **+16.2 cent** |
| 部分音 T60 (n=1 / 4 / 8、設計アンカー 2.0s/0.5s) | 2.00 / 1.73 / 1.20 s | **2.001 / 1.730 / 1.203 s** (R² = 1.0000) |
| 打弦点 1/8 のノッチ (第 8 部分音) | — | **約 −90 dB** (隣接 −21 dB に対し −111 dB) |
| 壁での接触時間 (木, v=0.5→6) | 0.1–1.0 ms・単調減少 | **0.28 → 0.17 ms** |
| 壁での接触時間 (フェルト, v=2) | ピアノの範囲 | **0.95 ms** |
| os=16 の収束 (対 os=64) | — | 木 **≤1.5 dB** / フェルト **≤0.5 dB** |
| デシメーション drop vs average | — | 部分音 0.01 dB 一致、フロア **−129 dB 以下** |
| モード打ち切りの波及 (60 本 vs 全 75 本) | — | **0.3 dB 以内** (20 本だと低次まで 1–4 dB 動く) |

打弦点 0.09 では第 11 部分音 (1/0.09 ≈ 11) がノッチに埋もれ、アナライザの走査が
隣の部分音を拾う (+174 cent と表示される)。**異常ではなくノッチの傍証。**

### Phase 6 — ROOM (X-Y)

| 項目 | 設計 | 実測 |
|---|---|---|
| 相互相関のピーク位置 (全体 / 直接音) | lag 0 | **lag 0** (0.951 / 0.954) |
| 部屋単体の残響相関 (インパルス、0.25 s 以降) | 0.5–0.8 | **0.4–0.85 の範囲内** (テスト固定) |
| 楽器込みの 2.5 s 以降の相関 | — | **0.983** — FDN ではなく鳴り続ける弦が支配。この楽器では正しい (best が lag −2 に 0.3% だけ出るのも同じ理由で、遅延差ではない) |
| ITDG (最初の反射) | 幾何どおり | **100–400 サンプル** (床反射、テスト固定) |
| 開き角 90° の側方 ±30° の L/R 差 | カーディオイド理論値 | **厳密一致** (式のテスト) |
| エンジン全体の最悪ケース CPU (全 44 ff + ROOM) | — | **484 µs = 36%** (締切 1333 µs、P8 で active スキップ) |

| 項目 | 値 |
|---|---|
| 生ピーク (撥 0.5 / 2.0 / 6.0 m/s) | **4.7 / 21.5 / 83.3 N** — スパイク支配 ([D-013](problems.md)) |
| クレストファクタ (スパイク / 持続部) | **20–40 倍** |
| `CALIBRATED_GAIN` (暫定) | **0.004** (ff 単音 ≈ −9 dBFS) |
| `process` のアロケーション | **0** (`assert_no_alloc` が ABI テスト全体で監視) |

### Phase 7 — 演奏表現 (2026-08-31)

| 項目 | 実測 |
|---|---|
| 打弦点掃引の重心 (C4 ff、x/L 0.03→0.30) | **1500 → 1258 → 1181 → 1043 → 1106 → 1024 → 897 Hz** (0.125 の落ち込みはノッチが第 8 部分音を消すコム効果) |
| ノッチ追随 | x/L=1/4 で第 4 部分音が第 3 の 5% 未満、1/8 で第 8 が消え第 4 は 10 倍以上復活 |
| 面の明るさ比 (生ブリッジ力、高次/低次) | wood 0.41 / felt 0.21 (響板通過後はさらに開く — ABI テストで 1/2 以下) |
| フルミュートの尾部 RMS (0.8 s 以降) | 開放の **1/20 未満** |
| 半音階の LUFS (補正前 → 後) | 広がり 9.9 LU (±4.8) → **±0.78 LU** (D-022) |
| 半音階の A4 M-max | −23.94 LUFS (15/14 は −22.23 — 楽器が違うので基準も別) |
| 全鍵 ff ヘッドルーム (半音階 37 鍵) | ピーク ≤ 1.0・>0.3 (テスト固定) |
| エンジン最悪ケース (このマシン、ROOM on) | 15/14 全 44 ff **127 µs** / 半音階全 48 ff **117 µs** — 半音階は位置が多いがモード総数が少なく軽い |

### Phase 10 前半 — 音域バランス (2026-08-31、ff・3 s・ROOM off・vel 1.0)

LUFS モーメンタリ最大 (dBFS はピーク)。ターゲット = `LUFS(A4) + 1.0 LU/oct`。
残差の許容は ±2.5 LU (`tests/register_balance.rs`)。

| 鍵 | f0 [Hz] | 補償前 M-max | 補償後 M-max | 残差 [LU] |
|---|---|---|---|---|
| G2 (43) | 98.0 | −26.40 | −24.41 | −0.0 |
| A2 (45) | 110.0 | −24.32 | −22.82 | +1.4 |
| B2 (47) | 123.5 | −29.66 | −24.36 | −0.3 |
| C3 (48) | 130.8 | −30.80 | −24.19 | −0.2 |
| D3 (50) | 146.8 | −31.27 | −23.98 | −0.2 |
| E3 (52) | 164.8 | **−32.65 (最小)** | −24.68 | −1.0 |
| F#3 (54) | 185.0 | −30.56 | −24.44 | −1.0 |
| G3 (55) | 196.0 | −26.85 | −21.67 | +1.7 |
| A3 (57) | 220.0 | −29.91 | −23.43 | −0.2 |
| B3 (59) | 246.9 | −30.06 | −23.72 | −0.7 |
| C4 (60) | 261.6 | −29.79 | −23.52 | −0.5 |
| D4 (62) | 293.7 | −28.84 | −22.72 | +0.1 |
| E4 (64) | 329.6 | −28.64 | −22.67 | −0.0 |
| F#4 (66) | 370.0 | −24.27 | −23.37 | −0.9 |
| G4 (67) | 392.0 | −26.01 | −23.52 | −1.1 |
| **A4 (69)** | 440.0 | **−22.23 (基準)** | **−22.23 (不動)** | 0.0 |
| B4 (71) | 493.9 | −16.44 | −20.58 | +1.5 |
| C5 (72) | 523.3 | −19.39 | −23.74 | −1.8 |
| C#5 (73) | 554.4 | −20.77 | −19.88 | **+2.0 (最大残差)** |
| D5 (74) | 587.3 | −16.25 | −20.95 | +0.9 |
| E5 (76) | 659.3 | −16.14 | −20.72 | +0.9 |
| F#5 (78) | 740.0 | −17.87 | −22.39 | −0.9 |
| G5 (79) | 784.0 | **−14.38 (最大)** | −19.84 | +1.6 |
| A5 (81) | 880.0 | −17.32 | −21.99 | −0.8 |
| B5 (83) | 987.8 | −15.97 | −20.56 | +0.5 |
| C#6 (85) | 1108.7 | −15.37 | −19.84 | +1.1 |
| D6 (86) | 1174.7 | −16.62 | −22.16 | −1.4 |

- 広がり: **18.3 LU → 4.8 LU**。単音 ff のピークは補償後 −9.2〜−17.4 dBFS
- 補償前の形は非単調: A2/G3 だけ箱の双峰 (110/195 Hz) に救われて浅い。
  ゆえにアンカー表 (`COURSE_GAIN_ANCHORS_DB`) も非単調 → [D-021](problems.md)
- 左専用鍵 (C#5/A5/B5/C#6/D6) はコース右側 f0 のゲインを受ける。
  コース共有対 (D5↔A5 等) の折衷は ±1.5 LU に収まった

### Phase 10 後半 — 面のラウドネス (2026-09-01、ff・3 s・ROOM off、木との差 [LU])

| 鍵 | レザー (補償前 → 後) | フェルト (補償前 → 後) |
|---|---|---|
| G2 (43) | +0.2 → +0.1 | +0.5 → +0.1 |
| E3 (52) | −3.4 → −0.6 | −3.2 → −0.6 |
| C4 (60) | −2.1 → +3.1 | −6.2 → +0.9 |
| A4 (69) | −7.6 → +1.0 | −14.3 → −2.2 |
| G5 (79) | −10.6 → +4.0 | −21.2 → −1.8 |
| D6 (86) | −15.1 → −2.6 | −7.7 → +2.6 |

- 校正は vel 1.0 / 0.5 の 2 点平均なので、単速度で見ると残差が ± に割れる
  (上の表は ff)。vel 0.5 では符号が反転してほぼ同じ絶対値になる
- 全鍵の最悪はレザー F#5 (78) の ±7.3 LU (速度特異点、D-026)

### Phase 3 — 楽器全体 (`render --table` で再掲できる)

| 項目 | 値 |
|---|---|
| 発音位置 / 音高の種類 | **44 / 27** (G2–D6、全音階) |
| 巻線の範囲 | バス全部 (w 1.7–3.9) + トレブル下 5 コース (w 1.4–2.2) |
| 応力 | 巻線弦 650 MPa (目標どおり) / 素線 534–957 MPa |
| 張力 | **86–154 N** (実機 60–110 N の文献に整合、やや強め) |
| インハーモニシティ B | **1.05e-4 (G2) → 8e-4 台 (トレブル左上端)** 単調増加 |
| 最大モード数 | **138** (G2、`MAX_MODES = 256` に収まる) |
| 基音 T60 | 12 s (G2) → 3 s (G5 コース) の対数補間 |
| **最悪ケース CPU** (全 44 位置 ff、64 サンプル) | **198 µs = 14.9%** (締切 1333 µs) |
| 無音時 CPU (active スキップなし) | 150 µs (Phase 8 で削る余地) |
| `note_on` のコスト | **34 ns** (係数の再構築は打弦点が変わったときだけ) |
| D-002 の再検 (hop 0.02) | 8 部分音ぜんぶ設計値どおり (R² = 1.0000)。**混在の劣化は無かった** |

---

## 5. 判断とその理由

**コードを見ても分からない「なぜそうしたか」。**

### 解析器を lib にした (bin の中の mod ではなく)

bin の中に閉じた解析器は `tests/` から叩けない。
参照音源を持たない本プロジェクトでは**測定器の正しさが唯一の足場**なので、
統合テスト (`tools/analyze/tests/roundtrip.rs`) から固定できる形にした。

### WAV はチャンネルを分けて持つ

Phase 0 ではモノしか書かないのに `Wav { channels: Vec<Vec<f32>> }` にしてある。
Phase 6 の ROOM で **L/R の相互相関そのものを測る**ため。後から直すと
「モノ化された値で調整してしまった」事故が起きうるので、最初から分けた。

`mono()` は**和ではなく平均**。X-Y のモノ互換性を見るとき、和と平均では
レベルが 6 dB 違う。テストで固定してある。

### `--peak` の既定を 0 にした

既定で正規化すると、A/B のたびにレベルが揃って差が消える事故が起きる。
注意力ではなく既定値で防ぐ。→ [D-004](problems.md)

### `--partial-t60` の下限を Phase 0 では直さなかった

適切な窓とホップは「測りたい T60 の範囲」で決まり、それは弦の減衰を
設計してから確定する。**先に決め打ちしない。** → [D-001](problems.md)

Phase 1 では当たらなかった (測った T60 は最短 1.2 s)。**Phase 3 の高音コースで
当たる見込み** — 5 kHz アンカー 0.8 s の設計だと高次部分音が 0.4 s を切る。

### 接触時間ではなくスペクトルで収束を判定した (Phase 1)

木の撥はチャタリングするので「接触時間の合計」が倍率に対して収束しない
(0.09 → 1.41 ms と増え続ける)。倍率の選定は**耳に届く観測量 = 部分音レベル**の
収束で行った。→ [D-010](problems.md) / [D-012](problems.md)

### 撥の再接触を許した

「最初の離脱で Released」はフェルト (1 回の接触で運動量を渡しきる)
専用の仮定だった。木の撥は弦に「逃げられて」23 µs で離脱し、まだ弦へ向かって
いるのに力を出せなくなっていた。離脱時に `velocity > 0` なら再接触を許し、
コストの上界として 20 ms で打ち切る。→ [D-012](problems.md)

### WAV 書き出しの重複を許した

共有すると「レンダラが解析ツールに依存する」筋の悪い依存方向になる。
30 行の重複のほうが安い。→ [D-003](problems.md)

### `CLAUDE.md` をリポジトリに持たせた

親ディレクトリの規約を継承する形にせず、単独で clone しても
鉄則が付いてくる形にした (独立方針)。

---

## 6. 次にやること

1. **Phase 9 前半の DAW 実機確認** (ユーザー、README のチェックリスト)。
   エディタの開閉・クリック発音 (停止中/再生中)・打鍵グロー・mic ドラッグ・
   restart チップ・125% スケーリングのズレ観察 (→ D-024 の実測)。
   追加で: 面を Leather/Felt に切り替えて音量が木と揃うか (D-026)、
   半音階 (Chromatic) で D#2–D#3 の低音弦ブロックが鳴り、鍵盤の下端
   4 鍵ぶんの拡張が表示されるか
2. **Phase 9 後半**: プリセットと `state` 拡張 (PhyPiano の `state.rs` が
   コピー元: 行ベースの寛容なテキスト形式、serde なし)
3. ~~Phase 8~~ **済** (2026-09-01、active スキップ — D-027)
4. **Phase 10 後半**: 音色の追い込み (D-020 の BRIDGE_HP など)

### 触るときに壊さないでほしいもの

- §4 の実測値の表 — 大きく動いたら測定器かモデルが壊れている
- `peak_defaults_to_no_normalisation` テスト — A/B 比較の安全装置
- `mono_is_the_average_not_the_sum` / `stereo_channels_do_not_swap` テスト —
  Phase 6 の X-Y 検証の前提
- `oversampling_is_converged_at_the_default` テスト — os=16 の収束の固定。
  **os=4/8 と比べる形に書き換えないこと** (そちらは収束していない)
- `faces_are_ordered_from_hard_to_soft` テスト — 撥の面の順序
- `note_off_does_not_stop_the_ring` (core と ABI の両方) — この楽器の定義
- `ringing_strings_do_not_survive_a_loop_restart` — ループ折り返しの安全装置
- ABI テストの `assert_no_alloc` — `Cargo.toml` の `default-features = false` を
  外すと **release で検査が消えて素通りになる** (既定機能の `disable_release`)
