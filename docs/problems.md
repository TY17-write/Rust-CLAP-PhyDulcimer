# 既知の問題・制約

本プロジェクトの問題を1箇所に集約する。**問題を見つけたらその場で追記する** (後回しにしない)。

リアルタイム制約ゆえに**意図的に諦めた近似**も「仕様上の制約」として記録する。
これが設計判断の記録になり、後から「なぜこうなっているのか」を追える。
**やらなかった判断こそ記録に値する。**

## 記入書式

```markdown
## [D-000] 一行で分かる表題
- 発生フェーズ: Phase N
- 分類: 音質 / 性能 / 検証手法 / 移植性 / 仕様上の制約 / 未解決
- 状態: 未対応 / 回避策あり / 恒久 / 修正済み (コミットへのリンク)
- 症状: 観測される事実
- 原因: 分かっていれば。**分からなければ「未特定」と書く**
- 回避策 / 対応方針:
- 参照: 論文・コミット・計画の該当節
```

---

## 一覧

| ID | 表題 | 分類 | 状態 |
|---|---|---|---|
| [D-001](#d-001-部分音の-t60-は-04-秒より短いと測れない) | 部分音の T60 は 0.4 秒より短いと測れない | 検証手法 | **対応済み** (Phase 3) |
| [D-002](#d-002-部分音が混在すると測れる-t60-の下限が上がる) | 部分音が混在すると測れる T60 の下限が上がる | 検証手法 | **解消** (D-001 と同根) |
| [D-003](#d-003-wav-の書き出しが-render-と-analyze-の-2-か所にある) | WAV の書き出しが render と analyze の 2 か所にある | 仕様上の制約 | 恒久 |
| [D-004](#d-004---peak-の既定は-0-正規化しない) | --peak の既定は 0 (正規化しない) | 仕様上の制約 | 恒久 |
| [D-005](#d-005-信号全体の-t60-はこの楽器では意味を持たない) | 信号全体の T60 はこの楽器では意味を持たない | 検証手法 | 恒久 |
| [D-006](#d-006-参照音源を持たない) | 参照音源を持たない | 仕様上の制約 | 恒久 |
| [D-007](#d-007-モデル本体がまだ無い) | モデル本体がまだ無い | 未解決 | **解消** (Phase 1) |
| [D-008](#d-008-simd-は-phase-8-を待たず移植した) | SIMD は Phase 8 を待たず移植した | 仕様上の制約 | 恒久 |
| [D-009](#d-009-共振器のホットループに累算器を足さない) | 共振器のホットループに累算器を足さない | 性能 | 恒久 (継承) |
| [D-010](#d-010-オーバーサンプルは-16-倍デシメーションフィルタは不要) | オーバーサンプルは 16 倍。デシメーションフィルタは不要 | 検証手法 | **測定済み** (Phase 1) |
| [D-011](#d-011-撥の剛性は物理定数ではなく接触時間からの校正値) | 撥の剛性は物理定数ではなく接触時間からの校正値 | 仕様上の制約 | 恒久 |
| [D-012](#d-012-木の撥はチャタリングし接触時間は収束した観測量ではない) | 木の撥はチャタリングし、接触時間は収束した観測量ではない | モデル | **対応済み** (再接触を許した) |
| [D-013](#d-013-出力の校正は暫定ブリッジ力は打撃スパイクが支配的) | 出力の校正は暫定。ブリッジ力は打撃スパイクが支配的 | 音質 / 仕様上の制約 | **解消** (Phase 5 で再校正。続きは D-020) |
| [D-014](#d-014-phase-2-の弦バンクは暫定形) | Phase 2 の弦バンクは暫定形 | 仕様上の制約 | **解消** (Phase 3–4) |
| [D-015](#d-015-パラメータの反映がブロック内のイベントに追い越されていた) | パラメータの反映がブロック内のイベントに追い越されていた | 不具合 | **修正済み** (Phase 2) |
| [D-016](#d-016-鳴っている弦への再打弦が発散していた-ループ再生で-lufs-66) | 鳴っている弦への再打弦が発散していた (ループ再生で LUFS +66) | 不具合 | **修正済み** (Phase 2、実機で発見) |
| [D-017](#d-017-全音階配置なので楽器に無い半音は鳴らないトレブル左は-2-cent) | 全音階配置なので楽器に無い半音は鳴らない。トレブル左は +2 cent | 仕様上の制約 | 恒久 |
| [D-018](#d-018-ブリッジ結合は接触力の順方向注入遅延帰還は原理的に発散する) | ブリッジ結合は接触力の順方向注入。遅延帰還は原理的に発散する | モデル | **測定済み** (Phase 4) |
| [D-019](#d-019-弦を回収しない楽器はデノーマルに沈む) | 弦を回収しない楽器はデノーマルに沈む | 性能 | **修正済み** (DC 注入) |
| [D-020](#d-020-響板と箱は校正値の塊で実測ではない) | 響板と箱は校正値の塊で、実測ではない | 音質 / 仕様上の制約 | Phase 10 で掃引 |
| [D-021](#d-021-音域バランスは-lufs-モーメンタリ最大で校正した) | 音域バランスは LUFS モーメンタリ最大で校正した | 音質 / 仕様上の制約 | **校正済み** (Phase 10 前半) |
| [D-022](#d-022-半音階配置のコースゲインは-1514-校正表--補正表) | 半音階配置のコースゲインは 15/14 校正表 + 補正表 | 音質 / 仕様上の制約 | **校正済み** (Phase 7) |
| [D-023](#d-023-パームミュートの減衰カーブは校正値) | パームミュートの減衰カーブは校正値 | 仕様上の制約 | 恒久 |
| [D-024](#d-024-エディタの-dpi-処理に既知の歪みがある-125150-スケーリング) | エディタの DPI 処理に既知の歪みがある (125%/150% スケーリング) | 移植性 | 既知 (実害報告待ち) |
| [D-025](#d-025-gui-スレッドとの受け渡しは-3-経路すべてロックなし) | GUI スレッドとの受け渡しは 3 経路すべてロックなし | 仕様上の制約 | 恒久 |

---

## [D-001] 部分音の T60 は 0.4 秒より短いと測れない

- **発生フェーズ**: Phase 0
- **分類**: 検証手法
- **状態**: **未対応** (Phase 1 で必要になったら直す)
- **症状**: `analyze --partial-t60` が短い減衰に対して `—` を返す。単一の減衰正弦波
  (1000 Hz) で T60 を振って測った境界:

  | 設計 T60 | 測定 | R² |
  |---|---|---|
  | 1.000 s | **1.000** | 1.0000 |
  | 0.600 s | **0.600** | 1.0000 |
  | 0.500 s | **0.500** | 1.0000 |
  | 0.375 s | — | — |
  | 0.250 s | — | — |

  **0.5 秒までは正確、0.375 秒から測れない。**
- **原因**: [`estimate_partial_t60`] は窓 200 ms / ホップ 50 ms が固定で、
  `fit_t60` は「ピークから −5 dB 落ちた点」から「−20 dB 落ちた点」までに直線を
  当てる。この 15 dB ぶんの時間は `T60/4` なので、T60 = 0.375 s なら 0.094 s =
  ホップ 2 個ぶんしかなく、`end <= start + 2` の条件で棄却される。
- **対応 (Phase 3)**: [`estimate_partial_t60_with`] で窓とホップを引数にした。
  CLI は `--t60-window` / `--t60-hop`。目安は **ホップ ≤ T60/12**。
  検証: T60 = 0.375 / 0.25 / 0.15 s がすべて誤差 10% 以内で測れることを
  テストで固定 (`short_t60_is_measurable_with_a_narrow_window`)。既定 (0.2/0.05)
  では従来どおり測れないことも同じテストで固定してある — **既定の沈黙は仕様**で、
  短い減衰を測るときは明示的に狭めること。
- **参照**: `tools/analyze/src/lib.rs` の `estimate_partial_t60_with` / `fit_t60`

## [D-002] 部分音が混在すると測れる T60 の下限が上がる

- **発生フェーズ**: Phase 0
- **分類**: 検証手法
- **状態**: **原因未特定**
- **症状**: 8 本の部分音を重ねた信号 (f0 = 220 Hz、`--t60 3.0 --t60-taper 1.0`、
  第 n 部分音の設計 T60 = 3.0/n) を測ると、n = 5 / 7 / 8 だけ `—` になる。

  | n | 設計 T60 | 測定 |
  |---|---|---|
  | 1 | 3.000 | **3.000** |
  | 2 | 1.500 | **1.500** |
  | 3 | 1.000 | **1.000** |
  | 4 | 0.750 | **0.750** |
  | 5 | 0.600 | — |
  | 6 | 0.500 | **0.500** |
  | 7 | 0.429 | — |
  | 8 | 0.375 | — |

  n = 7 / 8 は [D-001](#d-001-部分音の-t60-は-04-秒より短いと測れない) で説明が付く。
  **説明が付かないのは n = 5 (0.600 s) が落ちて n = 6 (0.500 s) が通ること。**
  単独の正弦波なら 0.600 s も 0.500 s も測れている (D-001 の表)。
- **原因 (Phase 3 で特定)**: **D-001 と同根のホップ量子化。** 当初の仮説 2 つ
  (隣接部分音の漏れ込み / 走査周波数のずれ) は**両方外れ**。当てはめ区間
  (−5 dB → −20 dB = T60/4) がホップ 50 ms で 3 点前後しか取れない境界では、
  ピークの位置がホップ格子のどこに乗るかで点数が 1 個増減し、`end <= start + 2`
  の棄却をまたぐ。n = 5 と n = 6 の違いは境界上の偶然だった。
- **検証**: 同じ WAV を `--t60-hop 0.02` で測り直すと、**8 本全部が設計値どおり
  (0.600 / 0.429 / 0.375 s、R² = 1.0000) に出た。** 混在による劣化は無い。
- **教訓**: **測定器が「測れない」と言ったとき、それを「値が無い」と読まないこと。**
  そして原因の仮説は書くだけでなく潰すこと — 2 つ書いてどちらも違った。
- **参照**: `tools/analyze/src/lib.rs`、D-001 の対応

## [D-003] WAV の書き出しが render と analyze の 2 か所にある

- **発生フェーズ**: Phase 0
- **分類**: 仕様上の制約
- **状態**: **恒久** (意図的)
- **症状**: `tools/render/src/main.rs` の `write_wav` と、`phydulcimer_analyze` の
  `write_wav` が同じことをしている (約 30 行)。
- **なぜそうしたか**: 共有すると **レンダラが解析ツールに依存する**という筋の悪い
  依存方向になる。逆に WAV I/O だけの crate を切ると、Phase 0 の時点で crate が
  1 つ増える。**30 行の重複のほうが安い。**
- **食い違ったときにどう気づくか**: `analyze` が `render` の出力を読めなくなるので、
  Phase 0 の完了条件 (`render` → `analyze` で設計値が読める) がそのまま検出器になる。
  加えて `tools/analyze/tests/roundtrip.rs` が lib 側の往復を固定している。
- **見直す条件**: プラグイン側 (Phase 2) でも WAV を書くようになったら、3 か所目に
  なる前に crate を切る。

## [D-004] --peak の既定は 0 (正規化しない)

- **発生フェーズ**: Phase 0
- **分類**: 仕様上の制約
- **状態**: **恒久** (意図的)
- **内容**: `render --peak` の既定を **0 (正規化しない)** にした。過去プロジェクトは 0.9。
- **理由**: 過去プロジェクトでは A/B 比較のたびに `--peak 0` を付け忘れ、**2 本のレベルが
  揃って差が消える**事故を繰り返した (引き継ぎメモに「頻出事故」として残っている)。
  既定を安全側に倒すほうが、注意力に頼るより確実。
- **副作用**: 出力が小さいまま書かれるので、そのまま聴くと音が小さい。聴くときは
  明示的に `--peak 0.9` を付ける。
- **固定方法**: `tools/render/src/main.rs` の `peak_defaults_to_no_normalisation`
  テストが 0 を固定している。**変えるならこのテストごと変えることになる。**

## [D-005] 信号全体の T60 はこの楽器では意味を持たない

- **発生フェーズ**: Phase 0
- **分類**: 検証手法
- **状態**: **恒久**
- **内容**: `analyze --t60` は信号全体の RMS 包絡に単一の指数を当てる。
  打弦楽器では部分音ごとに減衰時間が違うので、全体の包絡は
  「前半は高次部分音の速い減衰、後半は基音の遅い減衰」の 2 段になり、
  単一の指数では表せない。
- **ダルシマーではさらに悪い**: **ダンパーが無いので他の弦が共鳴して鳴り続ける。**
  1 音を鳴らしても、全体の包絡はその弦の減衰を表さない。
- **使ってよい範囲**: Phase 0 の疎通確認 (単一の減衰正弦波) まで。
  モデルが入ったら `--partial-t60` に切り替える。
- **参照**: `plan.html` の P10 (音色の追い込み)

## [D-006] 参照音源を持たない

- **発生フェーズ**: Phase 0 (計画時の決定)
- **分類**: 仕様上の制約
- **状態**: **恒久** (2026-08-31 に決定)
- **内容**: 実機の録音を用意しない。Pianoteq KIViR の Cimbalom は別の楽器
  (ダンパー付き・4 本弦) なので絶対比較には使わない。
- **影響**: 過去プロジェクトが音質の追い込みで使った主要な梃子 (`--compare` による
  「参照より何 dB」) が丸ごと無い。
- **代わりに何を基準にするか**: **物理量が文献値の範囲に入ること**が唯一の外部基準。
  線径・張力・接触時間・インハーモニシティ B を Phase 1 / Phase 3 の完了条件に
  置いてある。**ここを緩めると寄る辺が無くなる。**
- **参照**: `plan.html` §10、`research.md` §5

## [D-007] モデル本体がまだ無い

- **発生フェーズ**: Phase 0
- **分類**: 未解決
- **状態**: **解消** (Phase 1 で `modal` / `hammer` / `segment` が入った)
- **内容 (当時)**: `phydulcimer-core` にあるのは疎通確認用の減衰正弦波 (`smoke`) だけだった。
- **残っているもの**: ブリッジ結合 (Phase 4)、楽器全体 (Phase 3)、響板と箱 (Phase 5)、
  ROOM (Phase 6)。
- **参照**: `plan.html` §07 の実装フェーズ

## [D-008] SIMD は Phase 8 を待たず移植した

- **発生フェーズ**: Phase 1
- **分類**: 仕様上の制約 (計画からの逸脱)
- **状態**: **恒久**
- **内容**: 計画では「Phase 1 はスカラで正しさを確定、Phase 8 で SIMD 化」だったが、
  `modal.rs` は 過去プロジェクトで SIMD 化済み・テスト済みのものをそのまま移植した。
- **理由**: 動作実績のある実装をスカラに**書き戻す**ほうが、バグの入る余地と
  Phase 8 での二度手間を増やす。「スカラで正しさを確定する」の目的 (数値の検証) は
  移植したテスト (極の半径と角度・低周波の音高保持・T60) が担う。
- **注意**: Phase 8 の作業は「SIMD 化」ではなく「全弦常時走行の実測とチューニング」になる。
- **参照**: `crates/phydulcimer-core/src/modal.rs` 冒頭

## [D-009] 共振器のホットループに累算器を足さない

- **発生フェーズ**: Phase 1
- **分類**: 性能
- **状態**: **恒久** (指針として維持)
- **内容**: `ModalBank::process_sample` のループに出力以外の累算器を足すと、
  baseline (SSE2) ビルドでレジスタが尽きてスピルし、**別ループで回すより 1.4 倍遅く**なる
  (過去プロジェクトの実測: 融合 3.85 µs vs 別ループ 2.74 µs)。`f32x8` は AVX 無しだと
  SSE レジスタ 2 本を食う。
- **本プロジェクトでの帰結**: 縦振動用の `weighted_square_sum` は移植時に削除した
  (この楽器では縦振動を当面見送るため)。**将来ブリッジ結合 (Phase 4) で内積が
  要るときも、別ループで書くこと。**
- **参照**: `crates/phydulcimer-core/src/modal.rs` の `process_sample`

## [D-010] オーバーサンプルは 16 倍。デシメーションフィルタは不要

- **発生フェーズ**: Phase 1
- **分類**: 検証手法 / 性能
- **状態**: **測定済み** (2026-08-31)
- **測ったこと**: 接触中のオーバーサンプル倍率を 2–64 で振り、部分音レベル
  (アナライザ経由) を os=64 と比較した。

  | os | 木 (最大ずれ) | フェルト (最大ずれ) |
  |---|---|---|
  | 4 | +10 dB 以上 (全域) | 数 dB 明るすぎる |
  | 8 | **20 dB (第 10 部分音)** | 1.8 dB |
  | **16** | **≤ 1.5 dB** | **≤ 0.5 dB** |
  | 32 | ≤ 0.5 dB | ≤ 0.5 dB |

  **収束の判定は接触時間ではなくスペクトルで行った。** 木の接触はチャタリング
  ([D-012](#d-012-木の撥はチャタリングし接触時間は収束した観測量ではない)) で、
  接触時間の合計は倍率に対して収束しない (0.09 → 1.41 ms と増え続ける)。
- **決定**: 既定 16 倍。コストは接触中 (数 ms) だけなので許容。
- **デシメーションフィルタ**: **不要と判定。** drop (フィルタ無し) と average を
  比べ、部分音レベルは 0.01 dB まで一致、部分音の無い周波数のフロアは両方式とも
  **−129 dB 以下**。共振器バンクの状態は狭帯域で、ナイキスト超のエネルギーを
  そもそも持たない。「デシメーションフィルタは要るのか」への、この楽器での答え。
- **モード打ち切りの感度** (木・打弦点 0.05・os=16): 60 本で全数 (75 本) と
  0.3 dB 以内、40 本で最大 3.5 dB、20 本では**低次の部分音まで 1–4 dB 動く**。
  打ち切りは高域を消すだけでなく、接触のチャタリングを介して低次にも波及する。
  **演奏経路では絞らない。** 48 kHz のこの区間は全数 61–75 本で、`MAX_MODES = 256`
  に対して余裕がある。
- **参照**: `segment.rs` の `Segment::new` / `Decimation`、`render --string --os` で再測できる

## [D-011] 撥の剛性は物理定数ではなく接触時間からの校正値

- **発生フェーズ**: Phase 1
- **分類**: 仕様上の制約
- **状態**: **恒久**
- **内容**: `HammerParams::wood()` の `stiffness = 1e8` は、**剛体壁への接触時間が
  木の撥の実機レンジ (0.1–0.3 ms) に入るように掃引で決めた**。木材のヤング率から
  導いた値ではない。ヘルツ接触の等価剛性は接触面の曲率に強く依存し、撥の先端の
  丸みの実測が無い以上、独立には決められない。
- **校正の根拠** (剛体壁、v = 2 m/s、os = 64):

  | K | 接触時間 | ピーク力 |
  |---|---|---|
  | 1e12 (当初) | 0.005 ms | 6913 N ← 明らかに非物理 |
  | 1e9 | 0.084 ms | 435 N |
  | **1e8 (採用)** | **0.212 ms** | **173 N** |
  | 1e7 | 0.533 ms | 69 N |

  採用値はピーク力が弦の張力 (130 N) と同オーダーに収まる点も妥当。
  レザー 3e8 (0.67 ms)、フェルトは 過去プロジェクトの実測校正値 4.5e9 (0.95 ms) を維持。
- **面の順序はテストで固定**: 木 < レザー < フェルト (接触時間)。
- **参照**: `hammer.rs` の `HammerParams`、`render --contact-table --stiffness` で再測できる

## [D-012] 木の撥はチャタリングし、接触時間は収束した観測量ではない

- **発生フェーズ**: Phase 1
- **分類**: モデル
- **状態**: **対応済み** (再接触を許す形に変更)
- **症状**: 過去プロジェクトから移植した状態機械は**最初の離脱で Released に落として
  二度と触れさせない**。木の撥でこれを使うと接触が 23 µs で終わり、撥がまだ弦へ
  2 m/s で向かっているのに力を出せなくなる。倍率を変えると低次の部分音が 10 倍
  動く (os=4 と os=16 で 51.8 vs 4.4) という形で発覚した。
- **原因**: フェルト (接触 1–4 ms) は 1 回の接触で運動量を渡しきるので
  最初の離脱 = 打撃の終わりでよかった。木の撥は弦のほうが軽くて速く「逃げる」ため、
  1 回の接触では運動量のごく一部しか渡らない。実機の撥は弦の戻りに叩き返されるまで
  **複数回触れる** (チャタリング)。
- **対応**:
  1. 離脱時に撥がまだ弦へ向かっている (`velocity > 0`) なら `Approaching` に戻し、
     再接触を許す。弦から遠ざかる向きで離れたときだけ `Released`
  2. 活動窓が閉じる保証が無くなるので、`MAX_ACTIVE_SEC = 20 ms` で打ち切る
     (オーバーサンプルのコストを有界にする)。実測では遠端からの反射 (約 3 ms) が
     撥を跳ね返すので、通常この上限には達しない
- **帰結**: **「接触時間の合計」は木の撥では収束した観測量ではない** (倍率を上げる
  ほど微細な接触を数えて増える)。弦上の接触の検証は、フェルト面 (1 回で閉じる) の
  単調性と、木は「0.2–2 ms のオーダーに収まり、最終的に離れる」ことだけを固定した。
  剛体壁での接触時間 (こちらは収束する) は 0.1–1.0 ms・単調減少をテストで固定。
- **参照**: `hammer.rs` の `step` / `MAX_ACTIVE_SEC`、`segment.rs` のテスト

## [D-013] 出力の校正は暫定。ブリッジ力は打撃スパイクが支配的

- **発生フェーズ**: Phase 2
- **分類**: 音質 / 仕様上の制約
- **状態**: **解消** (Phase 5 でエンジン側に校正を移して取り直した。ただし
  「スパイクが支配的」という性質自体は響板を通しても残った — 続きは
  [D-020](#d-020-響板と箱は校正値の塊で実測ではない) の 3)
- **内容**: 楽器の出力はブリッジ力の和 [N] で、**打撃過渡のスパイクが支配的**。
  実測 (treble-short 相当、打弦点 0.09):

  | 撥の速度 | 生ピーク [N] | 持続部の部分音振幅 |
  |---|---|---|
  | 0.5 m/s | 4.7 | — |
  | 2.0 m/s | 21.5 | 約 1.5 |
  | 6.0 m/s | 83.3 | 約 2 |

  **クレストファクタが 20–40 倍**ある。ピークは速度に対してほぼ線形だが、
  持続部よりはるかに速く伸びる (高次ほど強い `∝ n` のブリッジ重みを、短い接触が
  フルに叩くため)。
- **Phase 2 の暫定対応**: プラグイン側 `CALIBRATED_GAIN = 0.004` (ff 単音の
  ピーク ≈ −9 dBFS)。そのぶん持続部は小さめに出ていた。
- **Phase 5 での解消**: 校正をエンジン側へ移し、響板を通した実測で取り直した
  (`engine.rs` の `CALIBRATED_GAIN`)。旧経路は `--raw` フラグとして残る。
- **測定上の教訓**: **打撃直後の窓と持続部の窓でピークを比べないこと。**
  同じ音でも桁が違う。`note_off_does_not_stop_the_ring` テストはこれで一度
  誤判定した (打撃過渡を含む窓 vs 含まない窓の比較になっていた)。
- **参照**: `engine.rs` の `CALIBRATED_GAIN`、D-020

## [D-014] Phase 2 の弦バンクは暫定形

- **発生フェーズ**: Phase 2
- **分類**: 仕様上の制約
- **状態**: **解消** (1・2 は Phase 3、3・4 は Phase 4)
- **内容**: CLAP 化を先に通すため、楽器は次の暫定形で置いてあった。
  1. クロマチック 44 鍵 → **Phase 3 で 15/14 の全音階配置に置き換えた**
     (`layout.rs`)。半音の欠落は仕様として残る ([D-017](#d-017-全音階配置なので楽器に無い半音は鳴らないトレブル左は-2-cent))
  2. 設計則が仮 (L·f0 一定) → **Phase 3 で実機の設計則に置き換えた**
     (`scaling.rs`: 台形の幾何・応力目標からの巻線導出・音域別の線径と減衰)
  3. コースあたり 1 本 → **Phase 4 で 2 本にした** (デチューン + 打撃ばらつき)
  4. ブリッジをまたぐ 2 区間の結合が無い → **Phase 4 で入れた**
     (接触力の順方向注入、[D-018](#d-018-ブリッジ結合は接触力の順方向注入遅延帰還は原理的に発散する))
- **参照**: `instrument.rs` / `layout.rs` / `scaling.rs` / `course.rs`

## [D-017] 全音階配置なので楽器に無い半音は鳴らない。トレブル左は +2 cent

- **発生フェーズ**: Phase 3
- **分類**: 仕様上の制約
- **状態**: **恒久** (実機の性質の再現)
- **内容**: 2 つの「バグに見える仕様」をここに固定する。

  **(a) 未マップの MIDI 鍵は無音。** 15/14 は全音階配置 (G/D メジャー) で、
  G#・Bb・D#・F などの半音は楽器に存在しない。鳴る音高は 44 位置で 27 種類。
  例外は D メジャーの導音 C# (C#5 / C#6 がトレブル左にある)。
  クロマチックな MIDI データを流すと**当該ノートだけ無音になるのが正しい**。
  埋めるための仮想弦は作らない (楽器でなくなる)。

  **代わりの E3–E6 半音階配置 (37 音) は Phase 7 で実装済み** (`Layout`
  パラメータ、activate 時適用)。バス 12 + トレブル 18 コースの半音間隔で、
  左 = 右 +7 半音 (半音列の +7 は半音列のまま — 共有弦の制約を保って
  隙間なく埋まる)。音域バランスの補正は D-022。

  **(b) トレブル左側は 12 平均律より +2 cent 高い。** ブリッジが弦長をちょうど
  2:3 に分けるので、左は右の**純正5度上 (702 cent)**。平均律の 700 cent との
  差がそのまま乗る。同じ音高がトレブル右にもある音では右 (平均律) が優先される
  ので、実際に +2 cent で鳴るのは**左にしか無い音** (C#5, A5, B5, C#6, D6)。
  実機の調律師は左右の妥協点にブリッジを動かす — それを再現する
  `Temperament` パラメータ (Pure Fifth ⇔ Equal) は **Phase 7 で実装済み**
  (`scaling::Temperament`、activate 時適用)。
- **配置の出典について**: 15/14 の音の並びは製作者により差がある。採ったのは
  Dusty Strings 系の標準的な 5 度間隔チューニングの**代表値**。表駆動なので
  差し替えられる。
- **参照**: `layout.rs`、`scaling.rs` の純正5度の導出、`render --table`

## [D-018] ブリッジ結合は接触力の順方向注入。遅延帰還は原理的に発散する

- **発生フェーズ**: Phase 4
- **分類**: モデル
- **状態**: **測定済み** (順方向注入を採用。真の双方向は将来の仕事)
- **試して発散した 2 案** (鳴っている両区間の力からブリッジ駆動を作って戻す):
  1. **変位比例** (バネ性支持点): `drive = −k·(F_R+F_L)`。k = 2e-5 で 0.05 秒後に 1e18
  2. **速度比例** (抵抗性支持点、LP 1.5 kHz + モード重み高域テーパ付き):
     k = 1e-3 でも 1 秒以内に発散
- **原因は弦の減衰の軽さ**: モードの固有減衰は 1 サンプルあたり σ·dt ≈ 1e-5 しか
  なく、1 サンプル遅延の帰還が持つ位相誤差の反減衰成分は、可聴レベルの結合強度で
  必ずこれを上回る。**明示的な (遅延つき) 帰還では、この楽器の弦は結合できない。**
- **採用した形**: **撥の接触力の順方向注入**。撥が片側を押している間、接触力を
  ブリッジ重み (`w_n` × 高域 2 次テーパ 1.2 kHz) で反対区間へ注入する。撥の力は
  駆動され続けないので環が閉じず、無条件で安定。校正は `DEFAULT_BRIDGE_COUPLING =
  1.5e-6` (逆数 ≈ 0.7 N/µm はブリッジ接触剛性として妥当なオーダー)、共鳴レベルは
  主音の **−24 dB** (掃引表は `course.rs`)。
- **失われるもの**: 打撃後の弱い持続的交換 (Weinreich 型ダブルデケイの微細構造)。
- **将来の道**: 注入が同サンプルの出力に現れる形へ組み替えた上での **1×1 陰的解**
  (後退オイラー的な自己項は無条件散逸)。`modal.rs` の注入を回転の前に移し、
  `β = Σ w_n·r·sinθ_n·g_n` で junction を解く。計画の「段階 B」はこの形でだけ成立する。
- **参照**: `course.rs` 冒頭、`segment.rs` の `BRIDGE_TAPER_HZ`

## [D-019] 弦を回収しない楽器はデノーマルに沈む

- **発生フェーズ**: Phase 4 (テストスイートが 30 秒 → 10 分超で発覚)
- **分類**: 性能
- **状態**: **修正済み**
- **症状**: 長い減衰を含むテストが 1 桁以上遅くなった。DAW でも長く鳴らすほど
  CPU が上がる不具合として現れるはずだった。
- **原因**: 減衰の尻尾が f32 のデノーマル域 (< 1.2e-38) を**何十秒もかけて通過し
  続ける**。x86 のデノーマル演算ペナルティは 1 命令あたり数十〜百サイクル。
  過去プロジェクトは「ボイス回収が先に効くので崖に到達しない」で FTZ を
  見送れたが、**回収の無いこの楽器では構造的に必ず当たる**。
- **対応**: 全共振器の入力へ微小 DC (1e-15 ≈ −300 dB) を常時注入する
  (`modal.rs` の `ANTI_DENORMAL`)。状態は 0 ではなく正規化数の微小定常値に
  落ち着き、デノーマルに沈まない。`forbid(unsafe_code)` を保ったまま FTZ/DAZ と
  同じ効果。テストスイートは 10 分超 → **46 秒**。
- **副作用**: 「無音 = 厳密 0」ではなくなった。無音判定のテストは閾値
  (1e-9 = −180 dB) 比較に変更。プラグインの Sleep 判定 (1e-6) には影響しない。
- **参照**: `modal.rs` の `ANTI_DENORMAL`

## [D-020] 響板と箱は校正値の塊で、実測ではない

- **発生フェーズ**: Phase 5
- **分類**: 音質 / 仕様上の制約
- **状態**: **Phase 10 で掃引して置き直す**
- **内容**: 響板 (`soundboard.rs`) と箱 (`cabinet.rs`) のパラメータは、
  実機の IR ではなく「もっともらしいバンドレベルの形」に合わせた校正値。
  経緯として重要なもの:
  1. **出力の速度化 (`tilt_exp`)**: 共振器の出力は変位相当で、そのままだと
     IR のバンドレベルが **126 Hz → 8 kHz で −100 dB 超**傾いた。放射は板の
     速度比例 (×f) だが、それでも足りず (−13 dB/oct 残る — 短い T60 の高域が
     1 秒平均で沈む + 共振ピークが σ に反比例)、実測で `tilt_exp = 2.5` に
     合わせて中高域を平坦 (−3 dB/oct) にした
  2. **箱の正規化**: 最初の見積もりでは双峰が中域フロアの +60 dB に突出。
     +17 dB まで下げた (`cabinet.rs` の `OUTPUT_NORM`)
  3. **クレストファクタは響板で下がらなかった** (D-013 の予想は外れ)。
     板自体が過渡で一斉に鳴るため、生 39 → 響板後 125 と**増えた**。
     打撃感が強いのはこの楽器の性格でもあるが、過剰なら P10 で
     ブリッジ力の整形 (弦ごとのハイパス等) を検討する
  4. **響板は 2 ゾーン独立バンク** (「1 枚の板」からの逸脱)。駆動点で
     モード重みが変わることの表現としては同等で、ROOM の 2 角度がタダで付く
  5. **未移植**: 弦ごとのブリッジ力ハイパス (過去プロジェクトの
     `BRIDGE_HP_RATIO`)。効果を測る参照が無いまま入れても切り分けを
     壊すだけなので、P10 の掃引と一緒に入れる
- **校正の入り口**: `render --soundboard` → `analyze --bands`。
  現行の形: 89 Hz から立ち上がり、126 Hz に箱の峰 (+17 dB)、
  中域 −134〜−136 dB で平坦、8 kHz へ −3 dB/oct。
- **教訓**: **窓なしの Goertzel で低域を読まない。** 高域の過渡の漏れ込みが
  100 Hz の読みを支配し、放射つまみのテストが一度無意味になった
  (過去プロジェクトの同じ教訓をここでも踏んだ)。
- **参照**: `soundboard.rs` / `cabinet.rs` / `engine.rs` の校正コメント、
  音域バランスのゲイン補償は D-021

## [D-021] 音域バランスは LUFS モーメンタリ最大で校正した

- **発生フェーズ**: Phase 10 (前半)
- **分類**: 音質 / 仕様上の制約
- **状態**: **校正済み** (`tests/register_balance.rs` が固定)
- **症状**: 高音域と低音域の音量差が大きい。全鍵掃引 (ff・3 s・ROOM off・
  vel 1.0) の LUFS モーメンタリ最大で **広がり 18.3 LU**
  (最小 E3 −32.7 / 最大 G5 −14.4、A4 −22.2 LUFS)。
- **原因**: ピッチ補償がどこにも無く、物理の傾きが積み上がっていた。
  1. ブリッジ出力重み `w_n = T·nπ/L` (`segment.rs`) — 弦長の短い高音で大
  2. 励振ゲインの `1/M_n = 2/(μL)` (`modal.rs`) — 軽い高音弦で大
  3. 響板の出力傾き `(f/1000)^2.5` + 放射効率 (`soundboard.rs`, D-020) —
     低音の基音を大幅に削る
  4. 打ち消す方向は箱の 110/195 Hz 双峰と低音の長い T60 だけ。
     **箱の双峰のせいで形は非単調** — A2 (110 Hz) と G3 (196 Hz) だけ浅く、
     130–330 Hz 帯が −7〜−9 LU の深い谷
- **対応**: `scaling::course_gain(f0)` — (log2 f, dB) 折れ線 14 アンカーの
  出力ゲインをコース単位で乗算 (`instrument.rs` の加算ループ、両経路)。
  **440 Hz は 0 dB** 固定で A4 の校正 (`CALIBRATED_GAIN`) は不動。
  補償後は全 27 鍵がターゲット直線
  `LUFS(A4) + 1.0 LU/oct · log2(f0/440)` の **±2.0 LU** (広がり 4.8 LU)。
- **指標の選択**: **ゲート付き積分 LUFS は使わない**。単音は減衰音で、
  積分値はレンダリング長と T60 に依存する (バス T60 12 s / トレブル 3 s の
  この楽器では低音を実態より大きく読む)。アタック近傍の 400 ms が支配する
  モーメンタリ最大を、条件固定 (ff・ROOM off) で比べる。
  測定器は `analyze --lufs` (BS.1770-4、規格テストで固定)。
- **退けた案**:
  - **ハンマー質量のピッチ依存** (実機は低音ほど重い、`hammer.rs` の註) —
    物理的には正しいが接触時間と音色が変わり、P1 の検証をやり直しになる。
    Phase 7 (ハンマー面) の仕事として保留
  - **響板 `tilt_exp` / 放射の変更** — 全音のスペクトルが動き、受容済みの
    A4 の音色まで変わる
  - **励振側のスケーリング** — ブリッジ結合の送り量も変わり、撥の非線形と
    干渉する。出力側なら物理は不変
  - **トレブル左区間のトリム** — 構造上は +3.5 dB (`L_left = 2L_right/3`) の
    はずが、実測では左専用鍵 (A5〜D6) は右側の高音鍵と同傾向で突出しない。
    測ってから触る、の逆 — **測ったら触らないで済んだ**
  - **D-020 の `BRIDGE_HP_RATIO` 移植** — レベルではなく過渡整形の問題で
    直交する。切り分けを保つため見送り (D-020 の 5 のまま)
- **ターゲットの傾き**: 完全フラットではなく **+1.0 LU/oct** (低音がやや
  控えめ)。実機の低音は高音より静かだが 18 LU も違わない、の緩い写し。
  許容 ±2.5 LU — 響板・箱のモード構造による鍵ごとの個性 (±2 LU) は
  音色として残す。
- **教訓**: 補償カーブが非単調なのはモデルの構造 (箱の双峰) がそのまま
  出ているためで、平滑な 2〜3 点補間では届かない。**アンカー表も校正値**
  (D-011 / D-020 と同じ扱い) — 響板・箱を触ったら掃引からやり直すこと。
- **参照**: `scaling.rs` の `COURSE_GAIN_ANCHORS_DB` / `course_gain`、
  `tests/register_balance.rs`、測定表は `docs/context.md`、
  掃引手順は `render --sweep` + `analyze --lufs`

## [D-022] 半音階配置のコースゲインは 15/14 校正表 + 補正表

- **発生フェーズ**: Phase 7
- **分類**: 音質 / 仕様上の制約
- **状態**: **校正済み** (`tests/register_balance.rs` の半音階テストが固定)
- **経緯**: D-021 のコースゲイン表は 15/14 の掃引から作った校正値。
  周波数パラメータ化されているので半音階にも近似的に移るはず、として
  まず共用して測った (2026-08-31、ff・3 s・ROOM off):
  - 共用のみ: 広がり 9.9 LU、ターゲット直線 ±2.5 LU を超える鍵が 8 つ
    (最大 F#5 +4.8 / C5 −4.6)。**系統的な傾きは移るが、鍵ごとの残差は
    弦の設計 (長さ・線密度) が違うぶん残る**
  - 補正表 (`CHROMATIC_GAIN_CORRECTION_DB`) を実測残差から作成:
    アンカー = 各コースの右側 f0 の 30 点 (ゲインはコース周波数でしか
    引かれないので実質ルックアップ)。共有コース対 (D#5↔A#5 等、左専用鍵は
    右側のゲインを受ける) は残差を折半
  - 補正後: **全 37 鍵が ±0.78 LU** (広がり 3.6 LU)
- **低音弦ブロックの追記 (2026-09-01、P10)**: 半音階に D#2–D#3 の
  ブロンズ巻 13 コースを追加した際、その音域のアンカー 13 点
  (77.78–155.56 Hz) を同じ流儀で追加した。補正前の残差は −5.1〜+3.3 LU
  (15/14 表が 98 Hz 未満で端の値に張り付くぶんの系統誤差を含む)。
  補正後は**全 50 鍵が ±0.79 LU**。既存の E3–E6 は弦設計ごと不変なので
  (published-ranges テストと `the_chromatic_bass_extension_is_bronze_wound_...`
  が固定)、既存アンカーには触っていない。
- **注意**: どちらの表も**響板・箱・弦設計に対する校正値**。それらを触ったら
  両配置とも `render --sweep --layout ... --no-room` + `analyze --lufs` の
  掃引からやり直すこと (D-021 と同じ)。
- **参照**: `scaling.rs` の `course_gain_for` / `CHROMATIC_GAIN_CORRECTION_DB`、
  測定表は `docs/context.md`

## [D-023] パームミュートの減衰カーブは校正値

- **発生フェーズ**: Phase 7
- **分類**: 仕様上の制約
- **状態**: 恒久 (D-011 / D-013 と同じ扱い)
- **内容**: `Segment::set_mute` のモード別追加減衰は物理モデルではなく、
  聴感に合わせた校正値:
  - フルミュート時の最上モード追加減衰 69 /s (T60 ≈ 0.1 s) —
    「すぐ死ぬが完全な無音ではない」
  - 基音側の重み下限 0.3 (基音 T60 ≈ 0.33 s) — 手のひらは腹を押さえるので
    基音も減衰するが、高次より遅い (フェルトと同じ向き)
  - モード次数に対して線形の重み。実機の手 (接触面積・押さえる位置) の
    測定は存在しないため、これ以上の精密化は根拠を持たない
- **実装の要点**: `ModalBank` のモード別減衰 (ホットループに既にある乗算)
  だけを書き換える。**`Segment::set_damping` は使わない** (全係数の再構築が
  走り、88 区間分は重すぎる)。解除 (amount = 0) は使用中の全モードに 1.0 を
  明示的に書き戻す — `set_active_modes` の端数レーンのゼロ埋めと干渉させない
  (回帰: `releasing_the_mute_restores_the_ring`)。
- **参照**: `segment.rs` の `MUTE_MAX_DECAY_PER_SEC` / `MUTE_FLOOR` /
  `set_mute`、`modal.rs` の `set_mode_damping`

## [D-024] エディタの DPI 処理に既知の歪みがある (125%/150% スケーリング)

- **発生フェーズ**: Phase 9 (前半)
- **分類**: 移植性
- **状態**: 既知 (実害の報告があってから直す)
- **内容**: CLAP の `gui.get_size` は Win32 では**物理 px** を返す約束だが、
  実装は保持した数値 (960×640) をそのまま返し、baseview へは
  `SystemScaleFactor` で**論理 px** として渡している。Windows の表示
  スケーリングが 100% なら一致するが、125%/150% では親枠とプラグイン
  ウィンドウの大きさが食い違う。
- **経緯**: 100% スケーリング環境の DAW では実害が出ないことを確認済み。
  直すなら `set_scale` で受けた倍率を保持して `get_size` に乗算する形だが、
  **測っていない環境の修正を先回りで入れない** (規約: 測ってから触る)。
- **参照**: `phydulcimer-plugin/src/editor.rs` のモジュールコメント

## [D-025] GUI スレッドとの受け渡しは 3 経路すべてロックなし

- **発生フェーズ**: Phase 9 (前半)
- **分類**: 仕様上の制約 (設計の記録)
- **状態**: 恒久
- **内容**: `egui-baseview` は自前のスレッドでイベントループを回すので、
  GUI ↔ プラグインの受け渡しはすべて `SharedState` (Arc) のロックフリー
  構造で行う。**GUI スレッドは DSP の状態に触らない。**
  1. **GUI → 音声のノート**: 自前 SPSC リング (`ring.rs`)。
     `[AtomicU64; 64]` + wrapping カウンタ、Acquire/Release。生産者は
     エディタのウィンドウスレッドだけ・消費者はオーディオスレッドだけ、
     が不変条件。**満杯は reject** (クリックは低レート、drop-oldest は
     SPSC の単純さを壊す)
  2. **音声 → GUI の打鍵フィードバック**: `strike_serials: [AtomicU32; 128]`。
     音声側は**実際に鳴らすときだけ** (`is_mapped` 確認後) 加算し、GUI は
     毎フレーム差分を見て 300 ms のグローをローカルに開始する。
     時刻はスレッド間で運ばない (シリアルだけ)
  3. **GUI → ホストのパラメータ通知**: `gui_edits: AtomicU32` ビットマスク
     (PARAMS の添字、11 < 32)。process 先頭で `ParamValueEvent` として
     出力イベントへ排出。**これが無いとホストのオートメーションと Undo が
     壊れる**
- **付随する判断 — エディタ表示中は Sleep しない**: プラグインが眠ると
  ホストが process を止め、鍵盤クリックのノートが排出されない。GUI
  スレッドから `request_process` を安全に呼ぶ手段が clack に無いため、
  `editor_open: AtomicBool` を見て**開いている間は Continue を返す**
  (コストは無音時 ~150 µs/block、閉じれば従来どおり眠る)。
- **参照**: `ring.rs` / `lib.rs` の `SharedState` / `editor.rs`、
  回帰は ring の単体テストと `assert_no_alloc` (排出経路も process 内)

## [D-026] 撥の面のラウドネス補償は静的な校正ゲイン (速度依存は残る)

- **発生フェーズ**: Phase 10 後半
- **分類**: 音質 / 仕様上の制約
- **状態**: **校正済み** (残差に既知の限界あり、下記)
- **経緯**: レザー・フェルト面は同じ打撃速度でも木より静かい。実測
  (2026-09-01、ff・3 s・ROOM off・LUFS モーメンタリ最大) では**音域に強く
  依存**し、バスはほぼ差なし (±3 LU)、トレブルは **10–21 LU** も凹む —
  柔らかい面は接触が長く、短い弦では接触時間が周期に近づいて高次部分音を
  励振できない (物理としては実機どおり)。音源としては面の切り替えで音量が
  跳ばないほうが使いやすいので、木と揃える補償を入れた。
- **実装**: `scaling.rs` の `face_gain` — **打った区間の f0** で引く
  アンカー表 (面ごと、15/14 全 27 鍵 = 実質ルックアップ、D-022 の流儀)。
  ゲインは `Segment` の打撃ゲイン (`set_strike_gain`) として励振に掛かる。
  **等価変換**: 注入力を g 倍し、撥が見る弦変位を 1/g する — 接触の動力学
  (チャタリング・接触時間) は g = 1 と厳密に同一のまま、その打撃の寄与
  だけが g 倍で出る (音色不変。単純に注入だけ g 倍すると弦が早く「逃げ」て
  動力学が変わる)。回帰: `strike_gain_scales_the_output_exactly` (g = 2 で
  サンプル単位に 2 倍)。
- **既知の限界** (静的ゲインの構造的なもの):
  - **速度依存**: 面ごとに速度→ラウドネス曲線の形が違うので、1 つの
    ゲインでは全強度で揃わない。校正は **vel 1.0 と 0.5 の 2 点平均** —
    残差は両速度でおおむね ±3 LU、レザーの F#5 だけ ±7.3 LU (この鍵は
    2 速度の差が 14.5 dB ある特異点)
  - **再打弦の相互作用**: 鳴っている弦を違うゲインで叩き直すとき、撥が
    見る既存振動の振幅が 1/g だけ食い違う (聴感上は微小)
  - **半音階の中間音**: アンカーは 15/14 の鍵。半音は隣接アンカーの補間で
    受けるが、鍵ごとの個性 (±5 dB) は補間できない
- **注意**: 表は**撥・弦設計・響板に対する校正値**。それらを触ったら
  3 面の掃引 (`render --instrument --sweep --face ... --no-room`) から
  やり直すこと。
- **参照**: `scaling.rs` の `FACE_GAIN_LEATHER_DB` / `FACE_GAIN_FELT_DB` /
  `face_gain`、`segment.rs` の `set_strike_gain`、`instrument.rs` の
  `note_on`、測定表は `docs/context.md`

## [D-027] active スキップ — 鳴っていない区間は眠る (P8)

- **発生フェーズ**: Phase 8 (**DAW の実機確認で発覚した実害への対処**)
- **分類**: 性能 / 仕様上の制約
- **状態**: **実装済み**
- **経緯**: 低音弦ブロック (D-022 追記) でモード総数が増え、**無音でも**
  半音階エンジンが 502 µs/block (38%、開発機) を食うようになった。
  エディタ表示中はプラグインが眠らない (D-025) ので、**エディタを開いた
  だけで GUI 操作が重くなる**という形で DAW に現れた。
- **実装** (`segment.rs`): 各区間が出力ピークを 1024 サンプル窓で見て、
  **閾値 1e-7 N 未満が 8 窓 (約 170 ms) 続いたら眠る**。眠っている間は
  バンクを回さず厳密に 0 を返し、`Instrument` はブロックごと飛ばす。
  起きる契機は 3 つだけ:
  1. **打弦** (`strike_delayed`)
  2. **ブリッジ駆動** (反対区間の撥の接触力の透過 — 起こさないと 5 度の
     共鳴が消える)
  3. reset 後の次の打弦 (reset 直後は眠る。生成直後も眠っている)
- **閾値は校正値**: 1e-7 N はエンジンの校正を通すとおよそ −140 dBFS。
  眠った瞬間に残る凍結状態もこのレベル以下で、聴感・24-bit フロアの下。
  撥が動いている間は眠らない (打撃直後の無音瞬間で誤判定しない)。
- **実測** (開発機、64 サンプル/block): 無音の 15/14 楽器 341 µs → **0.1 µs**、
  無音の半音階エンジン 502 µs → **32 µs** (残りは響板 ×2 + 箱 + ROOM)。
  全打鍵の最悪ケースは不変 (判定コストは数 µs)。
- **副作用と判断**:
  - 減衰し切った弦の出力が漸近ではなく**厳密に 0** になる。単調減衰の
    テスト 2 件は「0 に達した後の等号」を許す形にした
  - デノーマル対策 (D-019) は残す — 起きている弦が閾値の上で長時間
    漂う間はまだ DC 注入が要る
  - choke / 未打弦の区間が主な受益者。**鳴っている弦は眠らない** —
    ダンパーの無い楽器の物理はそのまま
- **参照**: `segment.rs` の `SLEEP_THRESHOLD_FORCE` / `track_activity` /
  `wake`、`instrument.rs` / `course.rs` のブロックスキップ、回帰は
  `a_fresh_segment_sleeps_until_struck` / `a_decayed_string_falls_back_asleep`

## [D-028] エディタの反映が数秒遅れる — egui-baseview は repaint 要求が無いと描画しない

- **発生フェーズ**: Phase 9 (**DAW の実機確認で発見** — P9 の完了条件が
  想定したとおり「自動テストでは出ない不具合」だった)
- **分類**: 不具合
- **状態**: **修正済み**
- **症状**: DAW でエディタのボタンを押してから表示に反映されるまで
  10 秒近く掛かる。音 (DSP) は正常で、CPU も低い。
- **原因**: `egui-baseview` 0.6 の `on_frame` は毎ティック egui のロジックを
  回すが、**実際の描画は「repaint 要求の期限が来たとき」だけ**
  (`repaint_after` のゲート)。マウス入力イベントもロジックを回すだけで
  描画はトリガしない。当エディタが repaint を要求していたのは打鍵グローの
  間 (16 ms) だけだったので、それ以外の操作はホバー等が偶発的に repaint を
  発生させるまで画面に出なかった。
- **なぜ自動テストで出なかったか**: テストは `Context::run_ui` で 1 フレーム
  実行するだけで、「次のフレームが描画されるか」は egui-baseview の
  ゲートの中にあり、ウィンドウを開かないと通らない経路だった。
- **対応**: `Editor::ui` の先頭で毎フレーム `ctx.request_repaint()` を呼ぶ —
  プラグイン GUI の定石 (表示中はフレームレートで描き続ける)。
  エディタを閉じればゼロコスト。グロー用の条件付き 16 ms 要求は不要に
  なったので削除。
- **回帰テスト**: `every_frame_requests_a_repaint` — フレーム出力の
  `repaint_delay` がゼロであることを固定 (要求を出し忘れると即座に落ちる)。
- **参照**: `phydulcimer-gui/src/editor.rs` の `ui`、
  egui-baseview 0.6 `window.rs` の `on_frame` (`do_repaint_now` のゲート)

## [D-029] ビルトインコンプ — 両手 (2 和音・ロール) の粒立ちのために

- **発生フェーズ**: Phase 10 後半
- **分類**: 音質 / 仕様上の制約
- **状態**: **実装・校正済み**
- **動機**: ダルシマーの主奏法は両手 — 2 和音とロール。ダンパーが無いので
  ロールでは鳴りが積み上がって新しい打撃を埋め、天井はソフトクリップが
  乱暴に受ける。個々の打撃を浮き出させたい (粒立ち)。
- **指標を先に作った**: `render --roll <HZ>` (鍵リストの循環連打) と
  `analyze --grain <HZ>` (打撃周期ごとの広帯域包絡 max/min 比の中央値)。
  ベースライン (ff 2 鍵ロール、ROOM off): 6 Hz 8.2 / 8 Hz 6.7 / 12 Hz 5.6 dB。
- **実測で分かったこと (設計の核心)**:
  1. **速いアタック (8 ms) は逆効果** — 打撃ピーク自体が捕まり粒立ちが
     下がった (6.7 → 5.9 dB)。アタックは 40 ms にしてピークを丸ごと逃がす
  2. **下方向コンプ単体では周期内の max/min は上がらない** — トラフの
     最小値は次打の直前 = ゲインが最も回復した位相にあるため。
     粒立ちを実際に上げるのは**トランジェント強調** (速い包絡 1.5/30 ms が
     遅い包絡を上回るオンセットだけ最大 +4 dB)
- **構成** (`compressor.rs`): しきい値 −24 dBFS・レシオ 3:1・ニー 6 dB・
  アタック 40 ms・リリース 180 ms (ロール間隔で部分回復)・メイクアップ
  +2 dB + オンセットブースト。検出は響板後のバス + トレブルの和 (モノ)、
  適用は両系統へ**同一ゲイン** — X-Y の性質 (遅延差ゼロ) を壊さない。
  ROOM の前なので残響尾はポンピングしない。
- **効果** (ff 2 鍵ロール、amount 1.0): 粒立ち 6 Hz 8.2→11.0 /
  8 Hz 6.7→8.7 / 12 Hz 5.6→6.9 dB。ラウドネス +1.9 LU。ff 和音の
  ピークは −3.2 dBFS でフルスケール内。
- **校正との関係**: **エンジンの既定は amount = 0 (厳密に素通し)** —
  音域バランス・面の補償・LUFS の校正体系はコンプ無しで測る (ROOM と
  同じ扱い)。プラグインのパラメータ `Comp` (id 12) の既定 0.5 が
  演奏体験にだけ乗せる。
- **参照**: `compressor.rs` (定数はすべて校正値)、`engine.rs` の配線、
  回帰は compressor 単体テスト 6 件 +
  `the_compressor_shapes_the_sound_and_stays_bounded`

## [D-015] パラメータの反映がブロック内のイベントに追い越されていた

- **発生フェーズ**: Phase 2
- **分類**: 不具合
- **状態**: **修正済み**
- **症状**: ABI テストで「Strike Position を変えて叩いてもノッチが動かない」
  「Level を変えても音量が同じ」。パラメータ変更と打鍵を**同じブロック**に
  入れると、打鍵が古い値を使っていた。
- **原因**: ゲインと打弦点を `process` の**ブロック先頭で 1 回だけ**読んでいた。
  CLAP のイベントはブロック内にサンプル精度で並ぶので、後から来た
  `ParamValueEvent` は次のブロックまで反映されない。DAW ではパラメータ変更と
  打鍵が同じブロックに乗ることは普通にある。
- **対応**: ゲインはイベントバッチを処理した**後**に読む。打弦点は
  `ParamValueEvent` を受けた場で楽器へ同期する (ただの store で、係数の再構築は
  次の打撃まで起きない)。
- **教訓**: ブロック単位のパラメータ読みは「ブロックの先頭」ではなく
  「イベント処理の後」。過去プロジェクトは毎バッチ `apply_params` を呼ぶ構造だった
  ので、この罠を踏んでいなかった。
- **参照**: `phydulcimer-plugin/src/lib.rs` の `process` / `handle_event`、
  ABI テスト `strike_position_parameter_moves_the_notch` が回帰を固定

## [D-016] 鳴っている弦への再打弦が発散していた (ループ再生で LUFS +66)

- **発生フェーズ**: Phase 2 (**DAW での実機確認で発見**)
- **分類**: 不具合
- **状態**: **修正済み**
- **症状**: egui-clap-host のループ再生で、周回のたびに音が大きくなり
  **LUFS +66 まで上昇**した。当初はホスト側の問題も疑われたが、こちらの
  物理のバグだった。再現テスト (ff で 0.1 秒おきに 30 連打) では
  **非有限値まで発散**した。
- **原因**: 撥が毎回**位置 0 (弦の静止位置) から出発**していた。鳴っている弦は
  打弦点が変位している (強打後で ±数 mm)。弦が撥側に変位している瞬間に叩くと、
  **出発の瞬間に有限の圧縮 `Δ = 0 − y_s` が生じ、`K·Δ^1.5` で数千 N の
  非物理的スパイク**が出る。音が大きいほど変位が大きく、変位が大きいほど
  スパイクが大きいので、ループ再生の再打弦が**正帰還**になる。

  変位 2 mm なら `1e8 × (2e-3)^1.5 ≈ 8900 N` — 通常の打撃のピーク力
  (数百 N) の 1 桁以上上。
- **なぜ自動テストで出なかったか**: 単発の打弦は静止した弦 (変位 0) から
  始まるので、この経路を通らない。`a_second_strike_adds_to_the_ringing_string`
  は連打しても「有限であること」しか見ていなかった。**発散は繰り返しの中で
  しか育たない** — 2 打では音量差として見えても、しきい値を割らない。
- **対応**: [`Hammer::strike_at`] を新設し、撥を**弦の現在変位から出発**させる。
  接触は圧縮 0 (弦の表面) から始まり、力は連続に立ち上がる。ダンパーの無い
  楽器では再打弦が常態なので、これは修正というより**この楽器に必須の物理**。
- **回帰テスト**:
  - core: `repeated_ff_strikes_converge_to_a_bounded_roll` — 30 連打で
    「単発の 50 倍未満」「20 打目 → 30 打目で 1.5 倍未満」に収束
  - ABI: `looped_restrikes_do_not_diverge` — ループ再現 (0.21 秒 × 40 打)
- **教訓**: **「有限である」のテストは発散の初期を素通しする。** 繰り返しの
  ある系は「増え続けていないこと」を定常に達した 2 点の比で固定すること。
- **参照**: `hammer.rs` の `strike_at`、`segment.rs` の `strike`
